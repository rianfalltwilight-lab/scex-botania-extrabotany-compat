# 1.4.2

适配 SCEX ExtraBotany 2.0-scex.8-dev 的独立森林法杖兼容层，Minecraft 1.21.1 / NeoForge。

保留花到发射器、花到魔力池、魔力池到 Manalink 的绑定和保存行为。本次配对版本更新不改变 1.4.1 的编译实现；不内置 Botania 或 ExtraBotany 依赖 JAR。

冻结交付记录：2/2 JUnit 通过，作为 scex.8 配套参与隔离客户端验证。早期版本的 7 条绑定保存断言是历史证据，不宣称在本版本独立重跑；完整整包和多人长期运行未验收。本次只公开源码和预发布附件，不部署生产。

构建依赖从公开 Maven 获取；不发布开发机绝对路径或第三方依赖二进制。运行 JAR 的代码和游戏资源与冻结交付一致，哈希及元数据打包差异见 release-manifest.json。
